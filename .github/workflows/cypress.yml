name: Cypress Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Rodar testes Cypress
        uses: cypress-io/github-action@v6
        env:
          CYPRESS_SWAG_USERNAME: ${{ secrets.CYPRESS_SWAG_USERNAME }}
          CYPRESS_SWAG_PASSWORD: ${{ secrets.CYPRESS_SWAG_PASSWORD }}