{
  "devDependencies": {
    "@faker-js/faker": "^10.6.0",
    "@types/node": "^26.2.0",
    "cypress": "^15.21.0",
    "mochawesome": "^8.0.1",
    "mochawesome-merge": "^5.1.1",
    "mochawesome-report-generator": "^6.3.2",
    "typescript": "^7.0.2"
  },
  
  "scripts": {
    "test": "cypress run",
    "posttest": "mochawesome-merge cypress/reports/mocha/*.json > cypress/reports/report.json && marge cypress/reports/report.json --reportDir cypress/reports --inline"
  }

}
